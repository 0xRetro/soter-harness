# OS
.DS_Store

# Node
node_modules/
dist/
*.log

# Soter private runtime checkpoints (never portable configuration)
.soter/state/

# Governed host realization outputs are private consumer state, never source.
/AGENTS.md
/CLAUDE.md
/.mcp.json
/.agents/
/.codex/
/.claude/
/.claude-plugin/

# Local planning tools (workshop scratch, never harness content)
nimbalyst-local/
