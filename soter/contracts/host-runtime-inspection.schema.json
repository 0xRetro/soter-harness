{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "soter://contracts/host-runtime-inspection/v1",
  "title": "Soter host runtime inspection",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "$contract",
    "contractVersion",
    "inspectedAt",
    "host",
    "server",
    "runtime",
    "hostRealization",
    "authority",
    "privacy",
    "inspectionFingerprint"
  ],
  "properties": {
    "$contract": { "const": "soter://contracts/host-runtime-inspection/v1" },
    "contractVersion": { "const": "1.0.0" },
    "inspectedAt": { "$ref": "#/$defs/instant" },
    "host": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "server": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "version", "startedAt"],
      "properties": {
        "name": { "const": "soter-core" },
        "version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" },
        "startedAt": { "$ref": "#/$defs/instant" }
      }
    },
    "runtime": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "state",
        "startupFingerprint",
        "currentFingerprint",
        "reasonCode",
        "restartRequired",
        "permittedNextAction"
      ],
      "properties": {
        "state": { "enum": ["current", "not-realized", "stale"] },
        "startupFingerprint": { "$ref": "#/$defs/nullableFingerprint" },
        "currentFingerprint": { "$ref": "#/$defs/nullableFingerprint" },
        "reasonCode": {
          "enum": [
            "SOTER_HOST_RUNTIME_CURRENT",
            "SOTER_HOST_RUNTIME_NOT_REALIZED",
            "SOTER_HOST_RUNTIME_STALE"
          ]
        },
        "restartRequired": { "type": ["boolean", "null"] },
        "permittedNextAction": {
          "enum": [
            "continue",
            "realize-host-runtime",
            "none",
            "restart-host-runtime"
          ]
        }
      },
      "oneOf": [
        {
          "properties": {
            "state": { "const": "current" },
            "startupFingerprint": { "$ref": "#/$defs/fingerprint" },
            "currentFingerprint": { "$ref": "#/$defs/fingerprint" },
            "reasonCode": { "const": "SOTER_HOST_RUNTIME_CURRENT" },
            "restartRequired": { "const": false },
            "permittedNextAction": { "const": "continue" }
          }
        },
        {
          "properties": {
            "state": { "const": "not-realized" },
            "startupFingerprint": { "const": null },
            "currentFingerprint": { "const": null },
            "reasonCode": { "const": "SOTER_HOST_RUNTIME_NOT_REALIZED" },
            "restartRequired": { "const": true },
            "permittedNextAction": { "const": "realize-host-runtime" }
          }
        },
        {
          "properties": {
            "state": { "const": "stale" },
            "startupFingerprint": { "$ref": "#/$defs/nullableFingerprint" },
            "currentFingerprint": { "const": null },
            "reasonCode": { "const": "SOTER_HOST_RUNTIME_STALE" },
            "restartRequired": { "const": null },
            "permittedNextAction": { "const": "none" }
          }
        },
        {
          "properties": {
            "state": { "const": "stale" },
            "startupFingerprint": { "$ref": "#/$defs/nullableFingerprint" },
            "currentFingerprint": { "$ref": "#/$defs/fingerprint" },
            "reasonCode": { "const": "SOTER_HOST_RUNTIME_STALE" },
            "restartRequired": { "const": true },
            "permittedNextAction": { "const": "restart-host-runtime" }
          }
        }
      ]
    },
    "hostRealization": {
      "type": "object",
      "additionalProperties": false,
      "required": ["state", "reasonCode", "permittedNextAction"],
      "properties": {
        "state": { "enum": ["current", "not-realized", "stale", "unavailable"] },
        "reasonCode": {
          "enum": [
            "HOST_REALIZATION_CURRENT",
            "HOST_REALIZATION_NOT_REALIZED",
            "HOST_REALIZATION_ACTIVE_LOCK_MISSING",
            "HOST_REALIZATION_ACTIVE_LOCK_STALE",
            "HOST_REALIZATION_MANIFEST_LOCK_STALE",
            "HOST_REALIZATION_APPLICABILITY_UNAVAILABLE"
          ]
        },
        "permittedNextAction": {
          "enum": [
            "continue",
            "realize-host-runtime",
            "refresh-active-configuration",
            "none"
          ]
        }
      },
      "oneOf": [
        {
          "properties": {
            "state": { "const": "current" },
            "reasonCode": { "const": "HOST_REALIZATION_CURRENT" },
            "permittedNextAction": { "const": "continue" }
          }
        },
        {
          "properties": {
            "state": { "const": "not-realized" },
            "reasonCode": { "const": "HOST_REALIZATION_NOT_REALIZED" },
            "permittedNextAction": { "const": "realize-host-runtime" }
          }
        },
        {
          "properties": {
            "state": { "const": "stale" },
            "reasonCode": { "const": "HOST_REALIZATION_ACTIVE_LOCK_MISSING" },
            "permittedNextAction": { "const": "refresh-active-configuration" }
          }
        },
        {
          "properties": {
            "state": { "const": "stale" },
            "reasonCode": { "const": "HOST_REALIZATION_ACTIVE_LOCK_STALE" },
            "permittedNextAction": { "const": "refresh-active-configuration" }
          }
        },
        {
          "properties": {
            "state": { "const": "stale" },
            "reasonCode": { "const": "HOST_REALIZATION_MANIFEST_LOCK_STALE" },
            "permittedNextAction": { "const": "realize-host-runtime" }
          }
        },
        {
          "properties": {
            "state": { "const": "unavailable" },
            "reasonCode": { "const": "HOST_REALIZATION_APPLICABILITY_UNAVAILABLE" },
            "permittedNextAction": { "const": "none" }
          }
        }
      ]
    },
    "authority": {
      "type": "object",
      "additionalProperties": false,
      "required": ["grants", "providerCallsPermitted", "writesPermitted"],
      "properties": {
        "grants": { "const": "none" },
        "providerCallsPermitted": { "const": false },
        "writesPermitted": { "const": false }
      }
    },
    "privacy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["credentialValuesIncluded", "providerResponsesIncluded", "privateStateIncluded"],
      "properties": {
        "credentialValuesIncluded": { "const": false },
        "providerResponsesIncluded": { "const": false },
        "privateStateIncluded": { "const": false }
      }
    },
    "inspectionFingerprint": { "$ref": "#/$defs/fingerprint" }
  },
  "$defs": {
    "instant": {
      "type": "string",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(?:[.][0-9]{3})?Z$"
    },
    "fingerprint": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    },
    "nullableFingerprint": {
      "type": ["string", "null"],
      "pattern": "^sha256:[a-f0-9]{64}$"
    }
  }
}
